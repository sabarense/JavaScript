let ultimo = vetor.pop();     // remove e retornar o último elemento do vetor
vetor.push(ultimo);           // insere um elemento no fim do vetor
let primeiro = vetor.shift(); // remove e retornar o primeiro elemento do vetor
vetor.unshift(primeiro);      // insere um elemento na primeira posição do vetor